<h1>商品一覧</h1>
<% @items.each do |item| %>

  <p>商品名</p>
  <%= link_to item_path(item.id) do %>
    <%= image_tag item.get_image %>
    <span><%= item.name %></span>
  <% end %>
  <td>￥<%= item.with_tax_price %></td>
<% end %>
